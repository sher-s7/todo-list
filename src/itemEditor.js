export const editTitle = (item, newTitle) => item.title = newTitle;
export const editDueDate = (item, newDate) => item.due_date = newDate;
export const editDescription = (item, newDesc) => item.description = newDesc;
export const editPriority = (item, newPriority) => item.priority = newPriority
export const editCompletion = (item, completed) => item.completed = completed;
